import { Meta, Story, Canvas } from '@storybook/addon-docs';
import { Info } from '@player-ui/reference-assets-plugin-react';
import { PlayerStory } from '@player-ui/storybook';
import { StoryWrapper } from '../components';
import infoFlow from '@player-ui/reference-assets-plugin-mocks/info/info-basic.json';

<Meta title="Reference Assets/Info" component={Info} />

# Info View

The Info view is an example of a top level asset that encomposes a form that a user might see.

<Canvas>
  <Story name="Info">
    <StoryWrapper>
      <PlayerStory flow={infoFlow} />
    </StoryWrapper>
  </Story>
</Canvas>