---
title: JVM-based Players
---

# Headless Player

The core Player platform is technically headless, meaning it doesn't contain any UI framework mechanisms. Each platform integration consumes the core Player, but adds a UI framework integration.

[Android](/platforms/android) is the primary JVM based Player that adds both Android View and Compose integration points for content rendering on Android. However, there are more JVM based Player integrations
