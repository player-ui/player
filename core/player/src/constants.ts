import type { PlayerRuntimeConfig } from "./types";

export const DEFAULT_RUNTIME_CONFIG: PlayerRuntimeConfig = {
  logLevel: "error",
  expressionStrictness: "error",
  duplicateIDStrictness: "error",
};
